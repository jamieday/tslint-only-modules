{
  "name": "tslint-only-modules",
  "version": "1.0.6",
  "description": "TSLint rule that restricts affected files to be modules",
  "main": "tslint-only-modules.json",
  "peerDependencies": {
    "tslint": "*",
    "typescript": "*"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.8",
    "tslint": "^6.1.2",
    "typescript": "^3.9.3",
    "typescript-tslint-plugin": "^0.5.5"
  },
  "scripts": {
    "copy": "cp -f package.json README.md LICENSE tslint-only-modules.json build/",
    "build": "tsc -p ./",
    "clean": "rm -rf build/"
  },
  "author": "Jamie Day <jamie@jamieday.ca>",
  "repository": {
    "type": "git",
    "url": "https://github.com/jamieday/tslint-only-modules.git"
  },
  "license": "MIT"
}
